VITE_BACKEND_URL="http://localhost:8000"
// VITE_BACKEND_URL="http://172.16.3.224:8000"
// Command to run in frontend:
// npm run build
// serve -s build/client -l 5173
// or keep running in background:
// screen -S frontend-serve -d -m bash -c "serve -s build/client -l 5173"
// kill switch:
// screen -S frontend-serve -X quit